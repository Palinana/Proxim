const FilterBar = () => {
    return (
        <div className="flex flex-wrap items-center gap-2">
            ServiceFilter
          {/* <ServiceFilter />
          <StatusFilter />
          <FindClosestButton />
          <ShareMapButton /> */}
        </div>
    );
}

export default FilterBar;